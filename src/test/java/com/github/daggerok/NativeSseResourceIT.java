package com.github.daggerok;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSseResourceIT extends SseResourceTest {

    // Execute the same tests but in native mode.
}
